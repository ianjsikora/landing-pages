/*
  ========================================
  Page Resize
  ========================================
*/
function resize_height_init(){
    (function($){
        $(".resize-height-full-page").height($(window).height());
    })(jQuery);
}
